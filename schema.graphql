schema {
	query: Query
	mutation: Mutation
}
type Query {
	getUser(id: ID!): User
}

type Mutation {
	addUser(id: ID!, name: string, book: Book): User
}

type User {
	id: ID!
	name: String!
	books: [Book]
}

type Book {
	title: String!
	author: String

}